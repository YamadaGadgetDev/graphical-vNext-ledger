# vNext Ledger — 設計判断を失わないための軽量台帳

vNext Ledger は、**コード中に埋もれ、消えていく設計判断を確実に記録することを目的として作成**されました。  
Jira や Notion の代替ではありません。**リポジトリそのものを一次情報源として扱うための軽量補助ツール**です。
これは「設計メモの保存ツール」ではなく、「判断の忘却を防ぐ装置」です。

---

## これは何か（What）

- `NOTE(vNext)` / `DONE(vNext)` などの **vNext 系コメント**をコードから自動収集  
- 出現箇所（ファイル / 行番号）を **SQLite 単一ファイル**に永続化  
- 差分スキャンと全走査（収束操作）を分離し、**事故らない運用**を保証  
- **CLI / HTTP API / Web GUI** 三層対応、CI 連携可能  

---

## これは何ではないか（What it is NOT）

- タスク管理ツールではありません  
- チケット駆動開発を置き換えません  
- 人を管理・リマインドする仕組みではありません  

**目的はただ一つ**  

> 「今じゃない判断」を、未来まで確実に残すこと

---

## 🔧 チケット管理との違い

| | チケット管理 | vNext Ledger |
|---|---|---|
| 管理対象 | タスク | 設計判断 |
| 入力コスト | 明示的（作業） | コメントを書く |
| 認知負荷 | 高い | 低い |
| 向いている場面 | 実装進行 | 「今やらない」判断の保存 |
| インターフェース | チケット = 粒度 | 台帳 = 全体観 |

vNext Ledger は **チケットの代替ではありません**。  
チケット化するには**重すぎる**、実装に着手するには**早すぎる**、
しかし**捨てるには惜しい判断**を、忘却せず保留するための台帳です。

---

## 背景にある問題意識

- TODO コメントは必ず死ぬ  
- 設計案は Slack / PR / 脳内で蒸発する  
- 半年後に「なぜこうしたか」が誰にも分からない  
- 結果、技術的負債は人の記憶に依存する  

vNext Ledger はこれを **構造で解決**します。

---

## 🔧 具体例：TODO コメントはどう死ぬか

### Before（よくある状況）

```python
# TODO: cfg周り、将来拡張考えると設計変えた方がいいかも
def load_config():
    ...
```

- どこにあるか分からなくなる  
- いつ書いたか分からない  
- なぜ今やらなかったか分からない  
- 他の TODO との関係が見えない  

結果、**誰も触らないまま忘却されます**。

---

### After（vNext Ledger を使うと）

```python
# NOTE(vNext): cfg-normalization
def load_config():
    ...
```

- 書いた瞬間に台帳へ記録される  
- 「今やらない」という判断が保存される  
- **GUI で `cfg` キーワード検索できる**
- **モーダルで詳細・コメント・修正履歴が一覧表示される**
- full scan を実行した時だけ世界が収束する  

👉 **TODO は記憶に依存するが、vNext は構造に残る。**

---

## 🔧 具体例：止血作業中の「賢い改善」が事故を起こすとき

- 本番で不具合が見つかり、止血対応をしている  
- 修正自体は小さい  
- しかし周辺コードを見て、こう思う  

```python
# NOTE(vNext): cfg-normalization
# （止血が終わった後で検討する）
```

> 「cfg周り、将来拡張を考えると整理した方がよさそう」

この判断自体は、**間違っていません**。問題は **「今やるべきではない」** という点です。

vNext Ledger は、**賢さを否定せず、タイミングだけを制御**します。

---

## 基本思想

### 1. 観測と収束を分離する

- **差分 scan（安全）**  
  - 変更されたファイルのみを観測  
  - stale / orphan には触らない  
- **full scan（収束）**  
  - 全走査 + 世界を閉じる操作  
  - stale / orphan の整理はこのときのみ  

👉 収束は「意図した操作」でしか起きません。

---

### 2. vNext は「立法府」である

- 思いつきは捨てない  
- しかし今は実装しない  
- **vNext に隔離して文明として保存する**

---

### 3. 人を責めない。文明を修復する。

- リマインドしない  
- 運用ルールを強制しない  
- **書いた瞬間に台帳へ入る構造**を作る  

---

## 🚀 起動・基本操作（GUI）

### 1. サーバー起動

```bash
python -m uvicorn app:app --host 0.0.0.0 --port 8000
```

ブラウザで `http://127.0.0.1:8000/` を開くと、ランディングページが表示されます。

```
認証が必要な場合：パスワード入力欄が右上に表示されます
（viewer / operator / dev / admin ロール対応）
```

---

### 2. ランディングページ（公開）

```
┌─────────────────────────────────────────┐
│ vNext Ledger — 設計判断を失わない台帳   │
│ AUTH_MODE=apikey                         │
├─────────────────────────────────────────┤
│ 📖 使い方                                │
│  • コードに NOTE(vNext): slug を書く     │
│  • 差分 scan で自動収集                  │
│  • GUI で確認・コメント・決定            │
│                                          │
│ 🔍 機能                                  │
│  • 台帳閲覧（テーブルビュー）            │
│  • キーワード検索                        │
│  • リスク・優先度評価                    │
│  • コメント・履歴の記録                  │
└─────────────────────────────────────────┘
```

---

## 📊 GUI レイアウト（メイン画面）

### レイアウト構成

```
┌─────────────────────────────────────────────────────┐
│  vNext Ledger        [🔍 検索] [ロール▼] [ログアウト]│
├─────────────────────────────────────────────────────┤
│ ┌────────────────────────────────────────────────┐  │
│ │ フィルター                                     │  │
│ │ ┌─────────────────────────────────────────┐   │  │
│ │ │ Status: [open] [doing] [parked] [done] │   │  │
│ │ │ Priority: [1] [2] [3]                   │   │  │
│ │ │ Risk: [none] [high] [critical]          │   │  │
│ │ └─────────────────────────────────────────┘   │  │
│ └────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────┤
│ 台帳テーブル                                        │
│ ┌──────────────┬──────┬─────────┬──────┬────────┐ │
│ │ Status       │ Pri  │ Slug    │ Ev   │ Actions│ │
├──────────────┼──────┼─────────┼──────┼────────┤ │
│ open         │ 2    │ cfg-... │ 3    │ [詳細] │ │
│ doing        │ 1    │ auth-.. │ 1    │ [詳細] │ │
│ parked       │ -    │ cache-. │ 5    │ [詳細] │ │
│ done         │ 3    │ ...     │ 2    │ [詳細] │ │
└──────────────┴──────┴─────────┴──────┴────────┘ │
├─────────────────────────────────────────────────────┤
│ ページネーション: « 1 2 3 4 5 » (全 47 件)          │
└─────────────────────────────────────────────────────┘
```

---

### テーブルカラム解説

| カラム | 説明 | ロール制限 |
|--------|------|----------|
| **Status** | open / doing / parked / done / stale | operator 以上で編集可 |
| **Priority** | 1~3（null = 未設定）| operator 以上で編集可 |
| **Slug** | 識別子（クリック可、詳細モーダルを開く） | 全員 |
| **Evidence** | 証拠数（ファイル位置・コメント数） | 全員 |
| **Actions** | [詳細表示] [編集] [削除] | ロール別 |

---

## 🎯 詳細モーダル（クリック時表示）

```
┌─────────────────────────────────────────────────────┐
│ 詳細: cfg-normalization                         [×]  │
├─────────────────────────────────────────────────────┤
│                                                      │
│ 【基本情報】                                         │
│  Status:  [open ▼]                                  │
│  Priority: [2   ▼]                                  │
│  Risk:    [high ▼]                                  │
│                                                      │
│ 【コメント】(operator 以上のみ入力可)                │
│ ┌──────────────────────────────────────────────┐   │
│ │                                              │   │
│ │ 将来の拡張を考えると、cfg の正規化が必要。  │   │
│ │ 今は止血優先で後回しにしている。            │   │
│ │                                              │   │
│ │                       [送信]                 │   │
│ └──────────────────────────────────────────────┘   │
│                                                      │
│ 【履歴】                                            │
│  2025-01-15 10:32 | status | open → doing          │
│  2025-01-14 18:45 | priority | - → 2               │
│  2025-01-14 15:22 | created                         │
│                                                      │
│ 【Evidence（発見地点）】                            │
│  • app.py:147 | def load_config()                   │
│  • config.py:23 | class ConfigLoader                │
│  • tests/test_cfg.py:89 | test_normalization       │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 🔍 検索・フィルター機能

### 全文検索

```
検索ボックス: [cfg-norm            ]
結果表示:
  ✓ cfg-normalization (3 件ヒット)
    - cfg.py:12
    - config.py:45
    - app.py:147
```

### 高度なフィルター

```
┌─ フィルター ─────────────────┐
│ Status:                       │
│   ☑ open   ☑ doing           │
│   ☐ parked ☑ done            │
│                               │
│ Priority:                     │
│   ☐ 1  ☑ 2  ☑ 3             │
│                               │
│ Risk:                         │
│   ☐ none ☑ high ☑ critical  │
│                               │
│ Evidence が N 件以上:          │
│   ☑ 1件以上 ☐ 3件以上        │
│                               │
│ [リセット] [適用]             │
└───────────────────────────────┘
```

---

## 📈 ダッシュボード・メトリクス表示

### サマリータブ

```
┌─────────────────────────────────────────────────────┐
│ 📊 サマリー                                          │
├─────────────────────────────────────────────────────┤
│                                                      │
│ 総数:           47 件                               │
│ 最終スキャン:   2025-01-15 14:32:15                │
│                                                      │
│ Status 別集計:                                      │
│  open    [████████░░░░░░░░░░░░] 15 件               │
│  doing   [████░░░░░░░░░░░░░░░░]  8 件               │
│  parked  [██░░░░░░░░░░░░░░░░░░]  3 件               │
│  done    [██████░░░░░░░░░░░░░░] 12 件               │
│  stale   [█░░░░░░░░░░░░░░░░░░░]  9 件               │
│                                                      │
│ Priority 別集計:                                     │
│  1       [████░░░░░░░░░░░░░░░░] 10 件               │
│  2       [██████░░░░░░░░░░░░░░] 18 件               │
│  3       [██████░░░░░░░░░░░░░░] 15 件               │
│  未設定  [██░░░░░░░░░░░░░░░░░░]  4 件               │
│                                                      │
│ Risk 別集計:                                        │
│  none     [█████████░░░░░░░░░░░] 20 件              │
│  high     [███████░░░░░░░░░░░░░] 18 件              │
│  critical [███░░░░░░░░░░░░░░░░░]  9 件              │
│                                                      │
└─────────────────────────────────────────────────────┘
```

### メトリクスタブ

```
┌─────────────────────────────────────────────────────┐
│ 📈 メトリクス                                        │
├─────────────────────────────────────────────────────┤
│                                                      │
│ Scan 実行履歴:                                      │
│  2025-01-15 14:32 | diff   | 3 ファイル | 0 追加   │
│  2025-01-14 18:15 | diff   | 2 ファイル | 1 追加   │
│  2025-01-13 10:00 | full   | 全走査     | 2 削除   │
│  2025-01-11 15:30 | diff   | 5 ファイル | 3 追加   │
│                                                      │
│ 最後の full scan: 2025-01-13 10:00                 │
│ diff scan 実行数: 24回（full 後）                  │
│                                                      │
│ Evidence トレンド:                                  │
│  1月    [████████░░░░░░░░] 84 個                   │
│  12月   [██████░░░░░░░░░░] 56 個                   │
│  11月   [████░░░░░░░░░░░░] 32 個                   │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 🔧 アクション操作（ロール別）

### Viewer（閲覧のみ）

```
詳細モーダル:
  ✓ Status / Priority / Risk 参照可
  ✓ コメント・履歴 参照可
  ✓ Evidence 参照可
  ✗ 編集・削除 不可
  ✗ コメント送信 不可
```

### Operator（編集可能）

```
詳細モーダル:
  ✓ Status 変更可（open ↔ doing ↔ parked ↔ done）
  ✓ Priority 変更可（1/2/3/未設定）
  ✓ Risk 変更可（none / high / critical）
  ✓ コメント送信可
  ✓ 履歴表示可
  ✗ 削除 不可
```

### Admin / Dev（全権限）

```
詳細モーダル:
  ✓ 全ての編集が可能
  ✓ ノート削除可
  ✓ 完全削除（purge）可
  ✓ ゴミ箱から復元可
```

---

## 🗑️ ゴミ箱管理

### ゴミ箱タブ

```
┌─────────────────────────────────────────────────────┐
│ 🗑️ ゴミ箱                                            │
├─────────────────────────────────────────────────────┤
│                                                      │
│ 削除済みノート (3 件)                               │
│                                                      │
│ ┌──────────┬──────┬──────────┬────────────────┐    │
│ │ Slug     │ Pri  │ Evidence │ Actions        │    │
├──────────┼──────┼──────────┼────────────────┤    │
│ old-cfg  │ 2    │ 2        │ [復元] [削除]   │    │
│ legacy   │ -    │ 1        │ [復元] [削除]   │    │
│ tmp-fix  │ 1    │ 4        │ [復元] [削除]   │    │
│                                                    │
│ ※ 完全削除（purge）はデータベースから復元不可    │
│                                                    │
└─────────────────────────────────────────────────────┘
```

---

## 🖥️ ロール切替UI（認証パネル）

```
┌─────────────────────────────────────────────────────┐
│ 右上：[👁 ロール: operator ▼]  [ログアウト]         │
│                                                      │
│ クリックでドロップダウン表示:                       │
│                                                      │
│  🔓 ロール選択                                      │
│  ┌────────────────────────────────────────────┐    │
│  │ 👓 viewer (閲覧のみ)       [パスワード入力]│    │
│  │ 👨‍💼 operator (編集可)       [パスワード入力]│    │
│  │ 👨‍💻 dev (全権限)           [パスワード入力]│    │
│  │ 🔐 admin (全権限)          [パスワード入力]│    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  各ロール専用パスワード入力 → [切替]               │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## ⚙️ Scan 操作UI

### Scan パネル

```
┌─────────────────────────────────────────────────────┐
│ Scan 操作                                            │
├─────────────────────────────────────────────────────┤
│                                                      │
│ 📁 スキャン対象ルート:                              │
│   /path/to/your/repo (設定から変更可)              │
│                                                      │
│ ┌─────────────────────────────────────────────┐    │
│ │ [🔄 差分 Scan] (安全)                       │    │
│ │ 変更されたファイルのみを観測               │    │
│ │                                             │    │
│ │ [🌍 全走査 Scan] (収束)                    │    │
│ │ ⚠️ 注意: この操作は不可逆です             │    │
│ │ stale / orphan の整理を含みます             │    │
│ │ CI では使用しないでください                │    │
│ └─────────────────────────────────────────────┘    │
│                                                      │
│ 最後の実行:                                        │
│  差分: 2025-01-15 14:32:15 (3 ファイル)           │
│  全走査: 2025-01-13 10:00                         │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 💾 エクスポート機能

### JSON エクスポート UI

```
┌─────────────────────────────────────────────────────┐
│ 📥 エクスポート                                      │
├─────────────────────────────────────────────────────┤
│                                                      │
│ 台帳データを機械可読形式で取得:                    │
│                                                      │
│ ┌─────────────────────────────────────────────┐    │
│ │ [📄 全ノート JSON] (notes.json)            │    │
│ │ [📊 サマリー JSON] (summary.json)          │    │
│ │ [📈 メトリクス JSON] (metrics.json)        │    │
│ │ [📜 Scan 履歴 JSON] (scan_history.json)   │    │
│ │ [🎁 一括DL (ZIP)]                          │    │
│ └─────────────────────────────────────────────┘    │
│                                                      │
│ 用途例:                                            │
│  • CI / 定期レビュー自動化                        │
│  • 外部ダッシュボードへの連携                      │
│  • 研究資料・報告書の生成                          │
│                                                      │
└─────────────────────────────────────────────────────┘
```

---

## 仕組み概要

### コメント規約（唯一の入力）

```python
# NOTE(vNext): login-flow
# DONE(vNext): entrypoint
```

- slug は小文字・英数・`-` / `_` 推奨  
- slug が **台帳の主キー**になります  

---

### 永続化される情報

- slug  
- 種別（NOTE / DONE）  
- ファイルパス  
- 行番号  
- 最終観測時刻  
- 状態（active / stale）  

すべて **SQLite 単一ファイル**に保存されます。

---

## 🚀 サーバー起動

```bash
python -m uvicorn app:app --host 0.0.0.0 --port 8000
```

ブラウザで `http://127.0.0.1:8000/` にアクセスして、GUI を使用します。

---

## 📦 CI 連携（スクリプト用）

GUI での手動操作が中心ですが、**自動化が必要な場合は以下をご参照ください。**

### 差分 Scan を定期実行（GitHub Actions 例）

```yaml
# .github/workflows/vnext-scan.yml
name: vNext Scan

on:
  push:
    branches: [main, develop]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Run vNext diff scan
        run: |
          curl -X POST http://localhost:8000/scan \
            -H "X-API-Key: ${{ secrets.VNEXT_API_KEY }}"
      
      - name: Export metrics
        run: |
          curl http://localhost:8000/export/metrics \
            -H "X-API-Key: ${{ secrets.VNEXT_API_KEY }}" \
            > metrics.json
      
      - name: Upload metrics
        uses: actions/upload-artifact@v3
        with:
          name: vnext-metrics
          path: metrics.json
```

### 定期的に JSON レポート生成

```bash
#!/bin/bash
# scripts/ci_export.sh

API_KEY=$1
OUTPUT_DIR="reports/$(date +%Y-%m-%d)"
mkdir -p "$OUTPUT_DIR"

# 差分 scan 実行
curl -X POST http://localhost:8000/scan \
  -H "X-API-Key: $API_KEY"

# JSON エクスポート
curl http://localhost:8000/export/notes \
  -H "X-API-Key: $API_KEY" > "$OUTPUT_DIR/notes.json"

curl http://localhost:8000/export/summary \
  -H "X-API-Key: $API_KEY" > "$OUTPUT_DIR/summary.json"

curl http://localhost:8000/export/metrics \
  -H "X-API-Key: $API_KEY" > "$OUTPUT_DIR/metrics.json"

echo "✓ 日付: $OUTPUT_DIR"
```

使用方法：
```bash
./scripts/ci_export.sh YOUR_API_KEY
```

---

### ⚠️ 全走査（full scan）について

**GUI の「全走査 Scan」ボタンはユーザーが明示的にクリックする操作**です。

```bash
# スクリプトでの使用は非推奨
curl -X POST "http://localhost:8000/scan?full=true"
```

理由：
- 不可逆な整合性確定を伴う
- stale / orphan の削除が確定される
- 意図しない削除を防ぐため、**GUI での手動実行に限定**

---

## 🔁 記録 → 観測 → 再検討 → 決着

vNext Ledger の想定フローは以下です：

1. コード中に `NOTE(vNext)` を書く
2. diff scan で 判断を観測（手動 or CI）
3. **GUI で台帳確認、詳細をモーダルで表示**
4. **操作パネルで Status / Priority / Risk 設定**
5. **コメント欄で判断根拠を記録**
6. 人間が再検討する
7. 決着したら `DONE(vNext)` に変える
8. full scan で 世界を収束させる

👉 **「判断は戻ってくる」構造**になっています。

---

## 📋 UI / UX 設計原則

### 1. 最小限の入力フィクション

- コード上の注釈だけで完全に管理
- GUI は **観測と再検討**の場に徹する

### 2. ロール別アクセス制御

- viewer : 閲覧のみ（情報共有）
- operator : 基本的な判定（Status / Priority / Risk）
- dev / admin : 削除含む全権限

### 3. 履歴の完全記録

- 全ての操作が events テーブルに記録される
- 「誰がいつ何を変えたか」が常に追跡可能

### 4. Evidence の自動リンク

- モーダル内に「この判断が見つかったファイル位置」を明示
- コード参照が一発で可能

---

## 🎯 実装チェックリスト（GUI 機能）

### 必須実装

- [ ] ランディングページ（公開）
- [ ] 認証・ロール切替パネル
- [ ] 台帳テーブルビュー（Status / Priority / Risk / Slug / Evidence カラム）
- [ ] 詳細モーダル（Status / Priority / Risk 編集、コメント送信）
- [ ] キーワード検索
- [ ] フィルター（Status / Priority / Risk）
- [ ] ゴミ箱タブ
- [ ] サマリータブ（集計グラフ）
- [ ] メトリクスタブ（Scan 履歴・トレンド）
- [ ] JSON エクスポートボタン
- [ ] Scan 操作パネル（差分 / 全走査）

### 拡張実装

- [ ] リアルタイム通知（新 NOTE 検出時）
- [ ] タイムライン表示（Evidence の日時軸）
- [ ] タグシステム（複数 slug への一括操作）
- [ ] カスタマイズ可能なダッシュボード
- [ ] ダーク / ライトモード切替
- [ ] モバイルレスポンシブ対応

---

## どんな人向けか

- 一人または少人数で大規模コードを守っている人  
- 設計判断を「忘却」ではなく「保存」したい人  
- TODO 管理に絶望したことがある人  
- Jira と設計思想を混ぜて地獄を見た人  
- **視覚的に判断を整理・再検討したい人**
- **ロール別のアクセス制御が必要な人**

---

## 逆に向かない人

- チケット駆動で全てを管理したい  
- 設計判断をドキュメントで完結させたい  
- 人にリマインドされたい  
- リアルタイムタスク割り当てが必要  

---

## 最後に

vNext Ledger は派手なツールではありません。  
しかしこれは、

> **「忘れないための構造」を持った極めて実務的な道具**

です。

GUI は、その構造を **見やすく、操作しやすく**するためのインターフェースに過ぎません。

- コードが一次情報源である
- 判断は時間とともに戻ってくる
- 世界を閉じる（full scan）は意図的な操作

この基本は、CLI でも GUI でも変わりません。

文明は、人が入れ替わり、去ったあとも続いて行くものです。  
vNext Ledger は、時間が経っても設計の意図だけは失われない状態を作るための道具です。
